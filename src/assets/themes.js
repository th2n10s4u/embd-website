import { createTheme } from "@mui/system"
import /*color I will be wanting to import for the "theme" of the site */ from '@mui/material/colors/';


const theme = createTheme({
    palette: {
      primary: /*color*/,
    },
    secondary: {
        
    }
  });